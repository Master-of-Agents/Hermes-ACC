---
schema_version: "1.0"
# Authoritative VPS inventory. Update this file after every host change.
# Scripts and runbooks read values from here; never hard-code host facts elsewhere.
servers:
  - id: srv1663264
    provider: hostinger
    hostname: srv1663264
    public_ip: 76.13.145.144
    admin_user: hermesctl
    ssh_port: 22
    os: "Ubuntu 22.04 LTS"  # assumption — confirm at bootstrap
    arch: x86_64
    timezone: UTC
    sudo_model: "passwordless for hermesctl (MVP — revisit if second user added)"
    docker_group: true
    firewall: ufw
    backup_recipient_age_public: "age1REPLACE_WITH_OPERATOR_PUBLIC_KEY"
    status: active
    purpose: "Primary Hermes agent hosting server"
    notes: "Hostinger VPS. Provisioned by Hostinger bootstrap. See vps/hostinger-srv1663264.md."
