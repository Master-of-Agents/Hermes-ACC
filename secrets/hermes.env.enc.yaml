# =============================================================================
# PLACEHOLDER — This file must be encrypted with sops + age before use.
# =============================================================================
#
# NEVER commit this file with real values. It must be encrypted by sops.
#
# To create the real encrypted file:
#   1. Complete age key setup: see secrets/README-age-key-bootstrap.md
#   2. Set real public keys in secrets/.sops.yaml
#   3. Run: sops secrets/hermes.env.enc.yaml
#   4. Enter real values in your editor — sops encrypts on save.
#
# To edit (once encrypted):
#   export SOPS_AGE_KEY_FILE="$HOME/.config/sops/age/keys.txt"
#   sops secrets/hermes.env.enc.yaml
#
# =============================================================================
TELEGRAM_BOT_TOKEN: REPLACE_WITH_SOPS_ENCRYPTED_VALUE
ANTHROPIC_API_KEY: REPLACE_WITH_SOPS_ENCRYPTED_VALUE
GITHUB_DEPLOY_KEY_B64: REPLACE_WITH_SOPS_ENCRYPTED_VALUE
# OPENROUTER_API_KEY: omitted — no account configured. Add when/if needed.
