---
schema_version: "1.0"
# Credential registry entry template.
# Copy the entry below into credentials/credential-registry.yaml.
# NEVER put actual secret values here.
credentials:
  - name: REPLACE_WITH_ENV_VAR_NAME
    provider: REPLACE_WITH_PROVIDER
    purpose: "REPLACE_WITH_ONE_LINE_PURPOSE"
    required_by:
      containers: []
      scripts: []
      agents: []
    storage_location: "secrets/hermes.env.enc.yaml :: REPLACE_WITH_ENV_VAR_NAME"
    encrypted_secret_ref: "sops:secrets/hermes.env.enc.yaml#REPLACE_WITH_ENV_VAR_NAME"
    runtime_destination: "/run/hermes/.env"
    rotation_frequency_days: 90
    last_rotated: "REPLACE_WITH_DATE"
    recovery_notes: "REPLACE_WITH_RECOVERY_INSTRUCTIONS"
    owner: primary-admin
    blast_radius: "REPLACE_WITH_BLAST_RADIUS_DESCRIPTION"
    dependencies: []
    revocation_procedure: "REPLACE_WITH_REVOCATION_STEPS"
    status: active
